/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['10.138.170.46', 'localhost:3000'],
};

export default nextConfig;
